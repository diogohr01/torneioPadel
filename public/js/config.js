/**
 * URL base da API.
 * - Em localhost: deixar vazio para usar o mesmo servidor (/api).
 * - No GitHub Pages: colocar aqui o URL do backend no Render, terminado em /api
 *   Ex.: 'https://torneio-padel-api.onrender.com/api'
 */
window.API_BASE_URL = '';
