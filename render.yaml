# Render Blueprint: deploy do backend (Node.js + Express + SQLite)
# Usar Docker garante build em Linux e evita "invalid ELF header" com better-sqlite3.

services:
  - type: web
    name: torneio-padel-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
